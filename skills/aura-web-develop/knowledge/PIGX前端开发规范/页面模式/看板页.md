# 页面模式：看板页

> 本模式必须同时遵守[样式布局与静态资源规范](../样式布局与静态资源规范.md)和[开发检查清单](../开发检查清单.md)。图表、地图和视频必须使用公开 resize 能力响应容器尺寸变化。

## 适用场景

- 首页/工作台/数据大盘
- 包含统计卡片、时间筛选、图表区、重点列表等多区块组合
- 数据以只读展示为主，不涉及复杂表单操作

---

## 页面结构

```
layout-padding
└── 时间筛选区（可选）
    ├── 统计卡片区（el-row + el-col，数值摘要卡）
    ├── 图表区（ECharts / D3 图表）
    ├── 快速跳转入口（快捷操作卡片）
    └── 告警/重点列表区（简化表格或卡片）
```

---

## 必须遵循

- 图表使用 `useEcharts` hook，不直接操作 ECharts 实例
- 统计卡片数据通过独立 API 接口获取，不依赖 `useTable`
- 看板页通常不需要分页和表单，避免引入不必要的 hook
- 图表 DOM 引用通过 `ref` + `onMounted` 初始化
- 响应式布局使用 `el-col :xs :sm :md :lg` 断点适配不同屏幕

---

## 推荐写法

### 统计卡片区

```vue
<el-row :gutter="20" class="mb20">
  <el-col :xs="24" :sm="12" :md="6" v-for="stat in statCards" :key="stat.key">
    <el-card shadow="hover" class="stat-card">
      <div class="stat-value">{{ stat.value }}</div>
      <div class="stat-label">{{ stat.label }}</div>
    </el-card>
  </el-col>
</el-row>
```

### ECharts 图表使用

```vue
<template>
  <div ref="chartRef" style="height: 300px; width: 100%"></div>
</template>

<script lang="ts" name="DashboardPage" setup>
import { useEcharts } from '/@/hooks/echarts';

const chartRef = ref<HTMLDivElement>();
const { setOption } = useEcharts(chartRef);

const loadChartData = async () => {
  const res = await fetchChartData();
  setOption({
    xAxis: { data: res.data.labels },
    series: [{ type: 'bar', data: res.data.values }],
  });
};

onMounted(() => {
  loadChartData();
});
</script>
```

### 看板页完整骨架

```vue
<template>
  <div class="layout-padding">
    <div class="layout-padding-auto layout-padding-view">
      <!-- 时间筛选 -->
      <div class="mb20 flex justify-between">
        <span class="page-title">数据看板</span>
        <el-date-picker v-model="dateRange" type="daterange" range-separator="至" @change="loadAll" />
      </div>

      <!-- 统计卡片 -->
      <el-row :gutter="20" class="mb20">
        <el-col :xs="24" :sm="12" :md="6">
          <el-card shadow="hover">
            <div class="stat-value">{{ stats.total }}</div>
            <div class="stat-label">总数量</div>
          </el-card>
        </el-col>
        <!-- 更多统计卡 -->
      </el-row>

      <!-- 图表区 -->
      <el-row :gutter="20" class="mb20">
        <el-col :span="16">
          <el-card header="趋势图">
            <div ref="trendChartRef" style="height: 300px" />
          </el-card>
        </el-col>
        <el-col :span="8">
          <el-card header="分布图">
            <div ref="pieChartRef" style="height: 300px" />
          </el-card>
        </el-col>
      </el-row>

      <!-- 重点列表 -->
      <el-card header="最新记录">
        <el-table :data="recentList" border size="small"
          :cell-style="{ textAlign: 'center' }"
          :header-cell-style="{ textAlign: 'center', background: 'var(--el-table-row-hover-bg-color)' }">
          <el-table-column prop="name" label="名称" />
          <el-table-column prop="createTime" label="时间" width="180" />
        </el-table>
      </el-card>
    </div>
  </div>
</template>

<script lang="ts" name="DashboardPage" setup>
import { useEcharts } from '/@/hooks/echarts';
import { fetchStats, fetchRecentList } from '/@/api/module/dashboard';

const trendChartRef = ref<HTMLDivElement>();
const pieChartRef = ref<HTMLDivElement>();
const { setOption: setTrendOption } = useEcharts(trendChartRef);
const { setOption: setPieOption } = useEcharts(pieChartRef);

const dateRange = ref([]);
const stats = ref({ total: 0 });
const recentList = ref([]);

const loadAll = async () => {
  const [statsRes, listRes] = await Promise.all([fetchStats(), fetchRecentList()]);
  stats.value = statsRes.data;
  recentList.value = listRes.data;
  setTrendOption({ /* ... */ });
  setPieOption({ /* ... */ });
};

onMounted(() => {
  loadAll();
});
</script>
```

---

## 常见拆分方式

- `index.vue` — 看板主容器（组合各区块）
- `components/StatCard.vue` — 统计卡片（如多处复用）
- `components/TrendChart.vue` — 图表组件（如图表逻辑复杂）
- `api/dashboard.ts` — 看板各区块的数据接口

---
