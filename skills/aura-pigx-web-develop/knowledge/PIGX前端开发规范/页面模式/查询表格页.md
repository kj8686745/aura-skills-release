# 页面模式：查询表格页

> 本模式必须同时遵守[工程与代码生成规范](../工程与代码生成规范.md)、[样式布局与静态资源规范](../样式布局与静态资源规范.md)和[开发检查清单](../开发检查清单.md)。表格列默认居中，需要差异化对齐时通过 `useTable({ tableAlign })` 配置。

## 适用场景

- 页面顶部有查询表单，中间有操作工具栏，主体是分页表格
- 常伴随新增/编辑弹窗（`form.vue`）
- 项目中 80% 以上业务页属于此类型

---

## 页面结构

```
layout-padding
└── layout-padding-auto layout-padding-view
    ├── 查询区（el-row + el-form，v-show="showSearch"）
    ├── 工具栏（el-row + 操作按钮 + right-toolbar）
    ├── 表格（el-table）
    └── 分页（Pagination 组件）
弹窗（form-dialog，放在最外层 layout-padding 内）
```

---

## 必须遵循

- 使用 `useTable(state)` hook 管理分页查询，不要手写分页逻辑
- `state.pageList` 必须指向 `api` 层的分页查询函数
- 删除操作必须先弹出确认框（`useMessageBox().confirm()`）
- 按钮权限使用 `v-auth="'permission_code'"` 指令
- 弹窗组件使用 `defineAsyncComponent` 异步引入
- 多选删除使用 `selectObjs` + `multiple` 控制禁用状态
- 导出使用 `downBlobFile` 方法，路径从 `api` 层维护
- 表格必须传入 `useTable(state)` 返回的 `tableStyle`，保持表头和单元格样式统一
- 多选列和序号列必须设置固定宽度：`type="selection"` 使用 `width="40"`，`type="index"` 使用 `width="60"`（文案较长时可固定为 `80`）
- 普通数据列默认添加 `show-overflow-tooltip`，避免长文本撑开表格
- 需要自定义渲染的列统一使用 `<template #default="scope">`，通过 `scope.row.xxx` 读取行数据

---

## 表格细节规范

### el-table 基础属性

分页表格统一使用 `state.dataList`、`state.loading` 和 `tableStyle`：

```vue
<el-table
  :data="state.dataList"
  v-loading="state.loading"
  style="width: 100%"
  row-key="id"
  border
  @selection-change="handleSelectionChange"
  @sort-change="sortChangeHandle"
  :cell-style="tableStyle.cellStyle"
  :header-cell-style="tableStyle.headerCellStyle"
>
  <el-table-column type="selection" width="40" align="center" />
  <el-table-column type="index" label="#" width="60" />
  <el-table-column prop="fieldName" label="字段名" show-overflow-tooltip />
  <el-table-column prop="status" label="状态" show-overflow-tooltip>
    <template #default="scope">
      <dict-tag :options="status_dict" :value="scope.row.status" />
    </template>
  </el-table-column>
</el-table>
```

### 列写法要求

- `type="selection"` 列固定写 `width="40"`，并设置 `align="center"`
- `type="index"` 列固定写 `width="60"`；如果国际化标题较长或页面已有固定视觉要求，可固定为 `width="80"`
- 普通文本列必须写 `show-overflow-tooltip`
- 字典、状态、时间格式化、操作按钮等自定义列也保留 `show-overflow-tooltip`，并使用 `#default="scope"`
- 不推荐使用 `#default="{ row }"` 作为生成默认写法，统一用 `scope.row`，便于复制和保持项目风格一致

---

## 推荐写法

### index.vue 完整骨架

```vue
<template>
  <div class="layout-padding">
    <div class="layout-padding-auto layout-padding-view">
      <!-- 查询区 -->
      <el-row class="ml10" v-show="showSearch">
        <el-form :inline="true" :model="state.queryForm" @keyup.enter="getDataList" ref="queryRef">
          <el-form-item label="字段名" prop="fieldName">
            <el-input v-model="state.queryForm.fieldName" style="max-width: 180px" />
          </el-form-item>
          <el-form-item>
            <el-button @click="getDataList" icon="search" type="primary">{{ $t('common.queryBtn') }}</el-button>
            <el-button @click="resetQuery" icon="Refresh">{{ $t('common.resetBtn') }}</el-button>
          </el-form-item>
        </el-form>
      </el-row>

      <!-- 工具栏 -->
      <el-row>
        <div class="mb8" style="width: 100%">
          <el-button v-auth="'xxx_add'" @click="formDialogRef.openDialog()" class="ml10" icon="folder-add" type="primary">
            {{ $t('common.addBtn') }}
          </el-button>
          <el-button v-auth="'xxx_del'" plain :disabled="multiple" @click="handleDelete(selectObjs)" class="ml10" icon="Delete" type="primary">
            {{ $t('common.delBtn') }}
          </el-button>
          <right-toolbar
            :export="'xxx_export'"
            @exportExcel="exportExcel"
            @queryTable="getDataList"
            class="ml10"
            style="float: right; margin-right: 20px"
            v-model:showSearch="showSearch"
          />
        </div>
      </el-row>

      <!-- 表格 -->
      <el-table
        :data="state.dataList"
        v-loading="state.loading"
        style="width: 100%"
        row-key="id"
        border
        @selection-change="handleSelectionChange"
        @sort-change="sortChangeHandle"
        :cell-style="tableStyle.cellStyle"
        :header-cell-style="tableStyle.headerCellStyle"
      >
        <el-table-column type="selection" width="40" align="center" />
        <el-table-column type="index" label="#" width="60" />
        <el-table-column prop="fieldName" label="字段名" show-overflow-tooltip />
        <!-- 字典标签列 -->
        <el-table-column prop="status" label="状态">
          <template #default="scope">
            <dict-tag :options="status_dict" :value="scope.row.status" />
          </template>
        </el-table-column>
        <!-- 操作列 -->
        <el-table-column label="操作" width="150">
          <template #default="scope">
            <el-button icon="edit-pen" @click="formDialogRef.openDialog(scope.row.id)" text type="primary" v-auth="'xxx_edit'">
              {{ $t('common.editBtn') }}
            </el-button>
            <el-button icon="delete" @click="handleDelete([scope.row.id])" text type="primary" v-auth="'xxx_del'">
              {{ $t('common.delBtn') }}
            </el-button>
          </template>
        </el-table-column>
      </el-table>

      <!-- 分页 -->
      <pagination @current-change="currentChangeHandle" @size-change="sizeChangeHandle" v-bind="state.pagination" />
    </div>

    <!-- 新增/编辑弹窗 -->
    <form-dialog @refresh="getDataList()" ref="formDialogRef" />
  </div>
</template>

<script lang="ts" name="ModulePageName" setup>
import { BasicTableProps, useTable } from '/@/hooks/table';
import { fetchList, delObj } from '/@/api/module/xxx';
import { useMessage, useMessageBox } from '/@/hooks/message';
import { useDict } from '/@/hooks/dict';
import { useI18n } from 'vue-i18n';

const FormDialog = defineAsyncComponent(() => import('./form.vue'));
const { t } = useI18n();

// 字典
const { status_dict } = useDict('status_dict');

// refs
const formDialogRef = ref();
const queryRef = ref();
const showSearch = ref(true);
const selectObjs = ref([]) as any;
const multiple = ref(true);

// 表格状态
const state: BasicTableProps = reactive<BasicTableProps>({
  queryForm: {},
  pageList: fetchList,
  descs: ['create_time'],
});

const { getDataList, currentChangeHandle, sizeChangeHandle, sortChangeHandle, downBlobFile, tableStyle } = useTable(state);

// 重置查询
const resetQuery = () => {
  queryRef.value?.resetFields();
  selectObjs.value = [];
  getDataList();
};

// 导出
const exportExcel = () => {
  downBlobFile('/module/xxx/export', Object.assign(state.queryForm, { ids: selectObjs }), 'xxx.xlsx');
};

// 多选
const handleSelectionChange = (objs: { id: string }[]) => {
  selectObjs.value = objs.map(({ id }) => id);
  multiple.value = !objs.length;
};

// 删除
const handleDelete = async (ids: string[]) => {
  try {
    await useMessageBox().confirm(t('common.delConfirmText'));
  } catch {
    return;
  }
  try {
    await delObj(ids);
    getDataList();
    useMessage().success(t('common.delSuccessText'));
  } catch (err: any) {
    useMessage().error(err.msg);
  }
};
</script>
```

---

## useTable 核心说明

`useTable(state)` 接收一个响应式对象，关键字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `pageList` | `Function` | 分页查询 API 函数（必填） |
| `queryForm` | `object` | 查询条件（会自动合并到请求参数） |
| `descs` | `string[]` | 默认降序字段（如 `['create_time']`） |
| `ascs` | `string[]` | 默认升序字段 |
| `isPage` | `boolean` | 是否分页，默认 true |
| `createdIsNeed` | `boolean` | 是否在组件创建时自动请求，默认 true |
| `props.item` | `string` | 响应数据中列表字段名，默认 `records` |
| `props.totalCount` | `string` | 响应数据中总数字段名，默认 `total` |

返回方法：

| 方法 | 说明 |
|------|------|
| `getDataList(refresh?)` | 刷新数据（传 false 不回第一页） |
| `currentChangeHandle(val)` | 翻页 |
| `sizeChangeHandle(val)` | 改每页条数 |
| `sortChangeHandle(column)` | 排序 |
| `downBlobFile(url, query, fileName)` | 导出文件 |
| `tableStyle` | 表格统一样式对象 |

常规查询表格页建议直接按以下顺序解构，后续模板、分页、排序和导出都复用这一组方法：

```typescript
const { getDataList, currentChangeHandle, sizeChangeHandle, sortChangeHandle, downBlobFile, tableStyle } = useTable(state);
```

- `getDataList`：查询按钮、重置后刷新、弹窗保存后刷新、右侧工具栏刷新都使用它
- `currentChangeHandle` / `sizeChangeHandle`：直接绑定到 `Pagination`
- `sortChangeHandle`：绑定到 `el-table` 的 `@sort-change`
- `downBlobFile`：导出 Excel 等文件时使用
- `tableStyle`：绑定到 `el-table` 的 `:cell-style` 和 `:header-cell-style`

---

## 常见拆分方式

- `index.vue` — 列表页主体
- `form.vue` — 新增/编辑弹窗（见弹窗表单规范）
- `api/xxx.ts` — `fetchList`、`getObj`、`addObj`、`putObj`、`delObj`
- `i18n/` — 模块国际化

---

